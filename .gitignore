# Cairo / Scarb
target/
**/*.rs
circuits/contracts/src/scarb-*/

# Rust build output
credit-scoring/target/

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
.venv/
.venv_py311/
.env
.env.local
*.egg-info/
.eggs/

# Node
node_modules/
.next/
.next_dev.log
out/
.npm
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build / deploy
*.env
!.env.example
.env.*
!.env.example

# ── Databases & runtime state (never track) ──
backend/data/test_portfolio_monitor_state_*.json
*.db
*.db-shm
*.db-wal
*.db-journal

# ── Credentials & session data ──
backend/data/session_keys.json
backend/data/issued_credentials.json
backend/data/credential_revocations.json
backend/app/data/session_keys.json

# ── Circuit test inputs with secrets ──
circuits/build/private_vote_input.json

# ── ZK proving keys (large binaries — use releases/LFS) ──
*.zkey
*.ptau

# Git worktrees (isolated branches)
.worktrees/

# Deployment tarballs
*.tar.gz
*.zip

# Large ML model files (EZKL proving keys - use external storage)
backend/app/data/ezkl_models/**/*.key
backend/app/data/ezkl_models/**/pk.key

# Broken / stale Next.js builds
frontend/.next.broken*/

# Scarb compiled binaries
circuits/contracts/src/scarb-*/bin/
circuits/build/**/*.ptau
circuits/build/**/accounts.json
circuits/build/**/.secrets
contracts/starkli_config/private_key.txt
backend/.env
.l1-sepolia-keystore.json
backend/.l1-sepolia-keystore.json

# Agent spam / backups (never track)
archive/
!frontend/src/app/archive/
!frontend/src/app/archive/page.tsx
frontend.backup*/
frontend.preview*/
*.bak
deploy_*_results.json

# Development status files
.HANDOFF-STATUS
.PHASE*-STATUS
.PHASE*-COMPLETE
.PROJECT-STATUS
*.COMPLETE.txt
TRADE_DESK_IMPLEMENTATION_COMPLETE.txt
.cursor/

# Deployment markers
*.deployed

# Stray images / Figma exports
*.png
!frontend/public/*.png
!docs-site/docs/public/*.png
!docs-site/docs/.vitepress/dist/*.png

# Generated artifacts
artifacts/

# Figma files
*.fig
receiptos/passport/node_modules/
receiptos/passport/.next/

# Starknet Foundry / Scarb caches (local builds)
**/.snfoundry_cache/

# VitePress (docs-site) dev cache
docs-site/docs/.vitepress/cache/

# TypeScript / ESLint incremental
*.tsbuildinfo
.eslintcache

# Local tooling (do not publish)
.codex
.codex/

# ── Backup / preview snapshots ──
frontend.backup*/
frontend.preview*/

# ── Deploy result artifacts (contain deployer keys context) ──
deploy_*_results.json

# ── Log files ──
*.log
!circuits/build/*.log

# ── README backups ──
*.bak

# ── Implementation status dumps ──
TRADE_DESK_IMPLEMENTATION_COMPLETE.txt

# ── PM2 ecosystem (local) ──
ecosystem.config.js
ecosystem.config.cjs

# ── Starkli wallet/account files ──
.starkli-wallets/

# ── Large EZKL KZG bundles (>50MB, regenerated from models) ──
backend/app/data/ezkl_models/yield_forecast/kzg_mpcheck_bundle.json
backend/app/data/ezkl_models/anomaly_detector/kzg_mpcheck_bundle.json

# ── Data backups ──
backend/data/*.backup_*

# ── Build logs ──
circuits/build/*.log

# ── Deployment markers ──
.marketplace_deployed

# ── Dead code archive ──
frontend/src/_archive/
