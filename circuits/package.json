{
  "name": "zkdefi-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circom circuits for Garaga private transfers",
  "scripts": {
    "build": "mkdir -p build && circom PrivateDeposit.circom --r1cs --wasm --sym -o build",
    "setup": "bash scripts/setup_groth16.sh",
    "prove": "node scripts/prove.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "garaga": "^1.0.1",
    "snarkjs": "^0.7.0"
  }
}
