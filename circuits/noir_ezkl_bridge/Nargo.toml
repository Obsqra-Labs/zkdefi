[package]
name = "noir_ezkl_bridge"
type = "bin"
authors = ["obsqra.xyz"]
compiler_version = ">=0.30.0"
description = "EZKL-style bridge circuit: model hash + output bounds (Noir → Garaga HONK)"

[dependencies]
