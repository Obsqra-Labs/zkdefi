# Frontend

Next.js frontend for zkde.fi (`/agent`, `/profile`, `/products`, `/trade`, `/vault`, `/lending`, `/oracle`).

## Local run

```bash
npm install
npm run dev
```

Default local port is `3001`.

## API base URL

Set `NEXT_PUBLIC_API_URL` in `.env.local` (for local backend usually `http://localhost:8003`).
